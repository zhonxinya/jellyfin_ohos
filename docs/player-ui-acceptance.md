# 播放页验收清单（播放 / 渲染 / 美化）

本文件用于**人工快速验收**播放页改动。分三部分：
① 已由设备实测确认的项（附证据）；② 需要人工目视/触控确认的项；
③ 已知限制与临时诊断件。

手动验收约需 **2–3 分钟**：播放任意视频后逐条对照即可。

---

## 一、已由设备实测确认（无需复查）

| 项 | 证据 |
|---|---|
| 软解渲染上屏 | 组件快照（componentSnapshot）取到真实视频画面：1260×2619、平均 RGB(77,66,58)、**254 色** |
| 硬解播放 | 服务端 `/Sessions`：`DirectStream`，位置持续推进 |
| 硬解失败自动回退软解 | 播 HEVC 内容 → `播放器错误` → 自动进入软解（`0:12 · 第 372 帧 · EGL 已渲染`） |
| 软解进度上报 | 探针文件 `player_progress.txt`：`{"code":204,"data":{"sent":true}}`（5 秒节流生效） |
| 后台播放 | 按 Home 后服务端位置仍推进（350.6s → 366.6s） |
| 进度行三栏布局 | dump 实测：Slider 宽 585px（左右各 72px 给时间）、时间与进度同行 |
| 控制条主/次分组 | dump 实测：`-10s/+10s` 在 y=2373，`1x/软解` 在 y=2586（分离） |
| 触达区 ≥48vp | dump 实测：主行按钮 357×162、次级 chip 147–188×162、顶部栏按钮 162×162 |
| 两行间距统一 12vp | dump 实测：次级行间距 41/40px（原 8vp≈24px） |
| 控制条自动隐藏 5s | 实测：唤出后 4.2s 控件仍在 |
| 播放中切换音轨 | 探针 + 播放器选中状态：`selectAudio ok track=2`，重开面板读回 `selected=[0,2,3]`（该条目含 2 条 AAC 音轨：粤语/国语）|
| 播放中切换字幕 | `subtitle embedded stream=4 ordinal=1 track=4` → `subtitle ok stream=4 track=4`；屏幕上字幕文本由**繁体**（`你沒事吧？`）变为**简体**（`你跳 我跳`）|
| 关闭字幕 | `subtitle off track=4`，字幕文本消失 |
| 切换**不打断**播放 | 全程位置持续推进（22202ms → 41175ms），无 `播放器错误`，服务端无会话重建 |
| 轨道面板渲染 | dump 实测：面板含「音频 / 字幕」标题、「关闭」、整行列表（`粤语 - Chi - AAC - Stereo - Default · aac · 2ch` 等）与 ✓ 选中标记 |
| 面板入口可点 | 点「音频/字幕」chip 后探针出现 `openSheet avAudio=2 jfStreams=5 soft=0` |
| 顶部栏锁定/解锁 | 探针 `player_state.txt`：`锁定 locked=1 controls=0` / `解锁 locked=0 controls=1`，界面出现「屏幕已锁定 + 中心圆形解锁按钮」|
| 顶部栏返回 | 点击后回到详情页 |
| 视频区手势可达 | `player_gesture.txt`：`gesture seekDeltaMs=286.8 volume=1.00 ... positionMs=…`（注入触摸确实进入手势层）|

> 切换实测环境：模拟器 `127.0.0.1:5555`，条目标题「阴阳路4：与鬼同行」（2 音轨 AAC + 2 条内嵌 SRT 字幕，MKV）。
> 「音轨切换」的证据是**播放器选中状态真的变了**（`getSelectedTracks()` 从 `[0,1,3]` 变为 `[0,2,3]`）；
> 切换后的**听感**无法自动化验证，见第二部分。

---

## 二、需要人工确认（我做不了，自动化手段在此失效）

### 1. 音轨切换的"听感"
- **操作**：播放「阴阳路4：与鬼同行」→ 控制条「音频/字幕」→ 点「国语」→ 再点「粤语」
- **预期**：对白语言随之切换（粤语 ↔ 国语），画面不中断、进度不跳
- **为什么需要人工**：自动化只能证明**播放器的轨道选中状态变了**（探针 + `getSelectedTracks`），
  听不出声音；设备侧也没有可回读的音频输出。

### 2. 视频区手势的观感
- **操作**：播放页 → 视频区横向滑动（seek）、左侧纵向滑动（亮度）、右侧纵向滑动（音量）、按住 0.6s（2× 快进）
- **预期**：出现带图标与数值的胶囊状反馈浮层；横向滑动松手后进度跳变
- **为什么需要人工**：浮层样式属观感判断（识别逻辑本身已由探针验证）。
- **已修缺陷（曾记录在此处）**：手势原按"每次 Move 事件的位移"判方向、阈值 5px，
  慢速滑动（每次事件位移 ≤5px）会被整段过滤、完全不响应。
  改为用**相对按下点的累计位移**判方向与幅度后，设备实测：
  500ms 滑完 500px 的**慢速**滑动 `seekDeltaMs=294.5`（修复前为 0）；
  右侧纵向下滑 `volume` 由 `1.00` 变为 `0.29`；点击视频区仍能正常显隐控制条。
  （注：`uitest` 注入的 swipe 本身有一定丢包率，同一手势重试即复现，不是应用问题。）

### 3. 加载态观感
- **操作**：连播多个视频，注意"打开播放器"的瞬间
- **预期**：显示「影片标题 + 类型 + 转圈 + `正在加载 · <播放器状态>`」，而不是空白黑屏
- **为什么需要人工**：该状态通常 <1 秒，抓取周期跟不上

### 4. 整体观感
- 请指出最不满意的 1–2 处（间距/字号/配色/层级），我优先修。

### 5. AVSession 媒体键反向控制（耳机按键 / 系统媒体控件）
- **操作**：播放中按耳机播放/暂停键，或用系统媒体控件（通知栏/锁屏）暂停
- **预期**：播放器暂停/恢复（已订阅 `play/pause/stop/seek` 并映射到本页控制）
- **为什么需要人工**：`uitest uiInput keyEvent 85`（媒体播放暂停键）**不会**触发 AVSession 的媒体键分发 ——
  设备实测按键只进入 `HMKeyboard_HardwareInputManager`（硬件输入通道），
  hilog 中**没有任何 AVSession 媒体键处理记录**；两次会话对照也确认播放未被暂停
  （`paused` 恒为 False、位置持续推进）。属**注入通道限制**，不代表功能失效。

---

## 三、已知限制与临时诊断件

- **软解模式下不能切换轨道**：软解会话只解码视频帧（无音频输出、无字幕解析），
  面板在这种情况下会明确提示"软解模式：轨道切换不可用"。
- **续播只在硬解路径生效**：详情页按钮显示「继续播放 · mm:ss」（取服务端
  `PlaybackPositionTicks`，设备实测与服务端一致：按钮 8:24 ↔ 504000ms、后为 5:00 ↔ 300000ms）；
  但内容一旦回退到软解，播放仍从片头开始（软解会话无"从指定位置起播"能力），
  界面已如实提示"软解暂不支持从上次位置续播，将从头播放"。
- **软解播放已不再触发 appfreeze**：此前 `RangeCache` 的同步 HTTP 跑在 UI 线程上
  （faultlog 主线程栈可证），逐帧拉取时主线程被阻塞 6 秒以上。现在"后台预取 + 帧拉取异步 +
  UI 线程渲染"三层处理，实测连续 70 秒软解播放：`0:28 · 第 842 帧 · EGL 已渲染 · 已上报进度`，
  无新增 appfreeze。注意 **EGL 渲染必须留在 UI 线程**（工作线程 swap 返回 `0x12301`，上不了屏）。
- **轨道切换的两条路径**：优先用播放器自身的轨道切换（`getTrackDescription` + `selectTrack`，
  不打断播放）；播放器没有暴露可切换轨道时（转码等）才回退到
  "改写 `AudioStreamIndex`/`SubtitleStreamIndex` → 重新取流 → seek 回原位"。
  **回退路径尚未在设备上触发过**（该条目上播放器总能给出轨道列表），只有编译验证。
- **临时诊断件（待清理）**：播放页的状态/进度/手势/轨道探针
  （`player_state.txt` / `player_progress.txt` / `player_gesture.txt` / `player_tracks.txt`）
  与 `native/core/http_client.cpp` 里 `ConnectTcp` 的调试 hilog。
  问题定性后应移除或改为调试开关。
- **服务端会话堆积**：`/Sessions` 曾达 60+ 条（多为我反复登录/安装的测试痕迹）；
  已在 `EntryAbility.onDestroy` 加"退出时上报停止"的兜底（仅编译验证，`aa force-stop` 不走生命周期回调）。

---

## 附：关于"合成触摸失效"结论的更正

早期版本的本文件曾写下两条"自动化在此失效"的结论，**现已被推翻并更正**：

| 早期结论 | 实际情况 |
|---|---|
| 顶部栏 `AppIconButton`（锁定按钮）对 `uitest` 点击无反应 | **可以点**。当时的点击发生在"控制条已自动隐藏"或"加载/错误全屏覆盖层"遮住顶栏的时刻 —— 覆盖层挡住是**正确行为**。控制条显示时点锁定按钮：界面出现「屏幕已锁定」，探针 `锁定 locked=1` |
| 视频区收不到注入触摸（ArkUI 透明层与原生 `DispatchTouchEvent` 都没收到） | **能收到**。手势层在 `TouchType.Up` 写入的探针有记录（`gesture seekDeltaMs=286.8 ...`）。当时"没反应"的真实原因是慢速滑动被 5px 阈值过滤（见第二部分） |
