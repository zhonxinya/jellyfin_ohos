Third-party dependencies for Jellyfin HMOS native

This directory holds vendored native libraries. For entries marked as vendored,
the license texts below are the ones to keep alongside the sources when
packaging distributions.

-------------------------------------------------------------------------------
mbedTLS
-------------------------------------------------------------------------------
Status: vendored at third_party/mbedtls (3.6.2, library/ + include/ only)
License: Apache-2.0 OR GPL-2.0-or-later (dual licensed)
Upstream: https://github.com/MbedTLS/mbedtls
Used by: native/core/http_tls.cpp (https:// Jellyfin servers)
TODO: copy LICENSE from the exact mbedTLS revision used by this project.

-------------------------------------------------------------------------------
nlohmann/json
-------------------------------------------------------------------------------
Status: single-header vendored at third_party/nlohmann/json.hpp (v3.11.3)
License: MIT
Upstream: https://github.com/nlohmann/json
TODO: keep LICENSE.MIT alongside the header when packaging distributions.

-------------------------------------------------------------------------------
dav1d（AV1 软解码）
-------------------------------------------------------------------------------
Status: **源码已内置**（native/third_party/dav1d/source/，未经修改的上游源码树），
        由本仓库脚本交叉编译为共享库并打进 HAP：
        scripts/build_dav1d_ohos.sh
        （编译产物已随仓库提交，克隆后构建应用直接链接；仅当指纹失效——源码树或脚本
          变化——时才会重编译，见 native/third_party/README.md 的 dav1d 一节）
版本:   dav1d 1.5.1
       下载地址: https://code.videolan.org/videolan/dav1d/-/archive/1.5.1/dav1d-1.5.1.tar.gz
许可证: BSD-2-Clause（见 native/third_party/dav1d/source/COPYING）
上游:   https://code.videolan.org/videolan/dav1d
用途:   AV1 软解码。FFmpeg 以 --enable-libdav1d 编译，`libavcodec.so` 的 DT_NEEDED 依赖
        `libdav1d.so.7`；播放侧对 AV1 显式优先选它（FFmpeg 自带的 `av1` 解码器在本构建里
        一帧都解不出来，根因见 native/feature/player/README.md 的「AV1 软解」）
源码:   native/third_party/dav1d/source/（随仓库提交，未经修改的上游源码）
构建产物: native/app/entry/libs/<abi>/libdav1d.so、libdav1d.so.7
          ABI: arm64-v8a（含汇编）、x86_64（纯 C，本环境无 nasm）
编译指纹: native/third_party/dav1d/build-stamp-<abi>.txt

-------------------------------------------------------------------------------
libcurl
-------------------------------------------------------------------------------
Status: not yet vendored
License: curl license (MIT/X-derived)
Upstream: https://curl.se/
TODO: copy COPYING from the exact libcurl revision used by this project.

-------------------------------------------------------------------------------
FFmpeg（软解码）
-------------------------------------------------------------------------------
Status: **源码已内置**（native/third_party/ffmpeg/source/，未经修改的上游源码树），
        由本仓库脚本交叉编译为共享库并打进 HAP：
        scripts/build_ffmpeg_ohos.sh
        （编译产物已随仓库提交，克隆后构建应用直接链接，不会重新编译；
          仅当指纹失效——源码树或脚本变化——时 CMake 才会自动调用该脚本，
          见 native/app/entry/src/main/cpp/CMakeLists.txt 的 FFmpeg 段与
          native/third_party/README.md）
版本:   FFmpeg 7.1
       对应上游源码包 ffmpeg-7.1.tar.xz
       sha256 = 40973d44970dbc83ef302b0609f2e74982be2d85916dd2ee7472d30678a7abe6
       下载地址: https://ffmpeg.org/releases/ffmpeg-7.1.tar.xz
许可证: LGPL-2.1-or-later
       （构建时已 `--disable-gpl` 语义下的配置：未启用任何 GPL 组件；
         如需确认，可用 `ffmpeg -buildconf` 或检查 configure 日志 --enable-gpl 未出现）
上游:   https://ffmpeg.org/  /  https://github.com/FFmpeg/FFmpeg
使用位置: native/feature/player/ffmpeg_decoder.cpp（软解：解容器 → 软件解码 → swscale 转 RGB）
源码:   native/third_party/ffmpeg/source/（随仓库提交，未经修改的上游源码）
构建产物: native/app/entry/libs/<abi>/libav{format,codec,util}.so*、libsw{scale,resample}.so*
          ABI: arm64-v8a、x86_64（两者均**随仓库提交**，见 .gitignore 的 FFmpeg 段）
编译指纹: native/third_party/ffmpeg/build-stamp-<abi>.txt
          （记录 FFmpeg 版本、ABI、构建脚本 sha256、内置源码树 git tree oid：
            证明仓库内的二进制就是仓库内的源码编译出来的，便于 LGPL 溯源）
公开头文件: native/third_party/ffmpeg/include/（随仓库提交，便于第三方按 LGPL 要求重建）

LGPL 合规说明（**动态链接**）:
  - 应用以**共享库动态链接**方式使用 FFmpeg，未做静态链接，用户可自行替换这些 .so；
  - 库文件未做任何修改（仅通过 configure 选项裁剪组件）；
  - **完整对应源码已随仓库提供**（native/third_party/ffmpeg/source/），并附可复现的构建脚本，
    据此可重建出二进制等价物（满足 LGPL 第 6 条的可获取性要求）；
  - 打包分发时需随包提供本段说明与 LGPL-2.1 全文（见第三段“分发清单”）。

分发清单（每次发布需随包提供）:
  1) LGPL-2.1-or-later 许可证全文（https://www.gnu.org/licenses/old-licenses/lgpl-2.1.txt）
  2) FFmpeg 版本号与上述源码获取方式（满足 LGPL 第 6 条的可获取性要求）
  3) 本 NOTICE 文件
 
-------------------------------------------------------------------------------
libass
-------------------------------------------------------------------------------
Status: not yet vendored
License: ISC
Upstream: https://github.com/libass/libass
TODO: copy COPYING from the exact libass revision used by this project.
