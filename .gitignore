# HarmonyOS native build output
native/app/node_modules/
native/app/oh_modules/
native/app/**/build/
native/app/**/.cxx/
native/app/.hvigor/
native/app/local.properties
local.properties
native/core/tests/out/

# FFmpeg（软解码）
# 源码随仓库提交：native/third_party/ffmpeg/source/（FFmpeg 7.1 原始源码，便于按 LGPL 要求重建）
# 公开头文件也随仓库提交：native/third_party/ffmpeg/include/
# 预编译共享库同样随仓库提交：native/app/entry/libs/{x86_64,arm64-v8a}/
#   —— 克隆后无需 HarmonyOS 工具链重编译 FFmpeg 即可构建（CI/协作者/离线环境）；
#      让产物失效并重建：bash scripts/build_ffmpeg_ohos.sh（或 FFMPEG_FORCE_REBUILD=1）
#      细节见 native/third_party/README.md「Prebuilt shared libraries are committed」一节。
# 防御：万一有人在源码树内直接 configure/make（脚本本身在临时工作目录构建，不产生这些）
native/third_party/ffmpeg/source/config.h
native/third_party/ffmpeg/source/config.asm
native/third_party/ffmpeg/source/ffbuild/config.*
native/third_party/ffmpeg/source/**/*.o
native/third_party/ffmpeg/source/**/*.d
native/third_party/ffmpeg/source/**/*.so
native/third_party/ffmpeg/source/**/*.so.*
native/third_party/ffmpeg/source/**/*.a

# dav1d（AV1 软解码，见 native/third_party/README.md）
# 源码随仓库提交：native/third_party/dav1d/source/（dav1d 1.5.1 原始源码）
# 公开头文件与预编译共享库同样随仓库提交（native/third_party/dav1d/include/、
# native/app/entry/libs/{x86_64,arm64-v8a}/libdav1d.so*）
# 防御：万一有人在源码树内直接 meson setup（脚本本身在临时目录构建，不产生这些）
native/third_party/dav1d/source/build/
native/third_party/dav1d/source/**/*.o
native/third_party/dav1d/source/**/*.so
native/third_party/dav1d/source/**/*.so.*
native/third_party/dav1d/source/**/*.a

# 签名材料与本机工程配置：禁止入库
# native/app/build-profile.json5 由 DevEco 自动签名写入，含本机证书路径与密码；
# 模板见 build-profile.json5.template，CI 使用 build-profile.ci.json5。
native/app/build-profile.json5
*.p12
*.cer
*.p7b
*.csr
.ohos/

# Local IDE state
.idea/

# Editor / local artifacts
.vscode/
*.jpeg
*.jpg
*.log
screen.png

# Build artifacts
.native-build-debug.exitcode
.native-build-debug.log

# Legacy root build output
build/

# 设备实测证据（截图/解出的首帧等），不入库
ui-screen*.jpeg
ui-softdecode-firstframe.png
ui-*.png
ui-*.jpeg

# 本地参考用的 Jellyfin 服务端源码（tarball + 解压目录）：仅作查阅，不入库
docs/jellyfin-10.8.12/
docs/v10.8.12.tar.gz

# 本地 Agent 配置，不随仓库发布
AGENTS.md
.agents/

# Local agent tooling (session plans, etc.)
.zcode/

# Git worktrees (local isolation)
.worktrees/

